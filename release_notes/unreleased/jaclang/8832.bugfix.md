- **Fix: a program nested too deeply to compile says so**: A very long operator chain or a deeply nested expression used to fail with an internal "maximum recursion depth exceeded" message and no file or line. It now reports `E0111`, naming the file and suggesting the expression be split or built in a loop. A program that recurses while it runs still reports its own failure with its traceback.

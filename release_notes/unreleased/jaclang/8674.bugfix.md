- **Fix: looking up an enum member by name type-checks**: `Tokens["A"]` on an enum no longer fails with `Cannot assign Tokens[Literal["A"]] to Tokens`, and now has the enum's own type. More generally, subscripting a class calls its metaclass `__getitem__` when one is defined, as Python does. Generic classes like `Box[int]` still specialize as before.
